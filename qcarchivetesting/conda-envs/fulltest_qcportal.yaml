name: qcarchive-qcportal
channels:
  - defaults
  - conda-forge

dependencies:
  - pip

  # NOTE: msgpack-python in conda is msgpack in pypi (due to a rename around v0.5)
  - numpy
  - msgpack-python
  - requests
  - pyyaml
  - pydantic<2.0
  - zstandard
  - qcelemental
  - tabulate
  - tqdm
  - pandas
  - pyjwt