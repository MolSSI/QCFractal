name: test
channels:
  - psi4
dependencies:
    # Compute
  - psi4

    # Base depends
  - python
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema
  - bcrypt
  - cryptography
  - mongoengine

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - pydantic
    - git+git://github.com/MolSSI/QCEngine@v0.4.0#egg=qcengine

    # Fireworks
    - fireworks
    - codecov
