name: test
channels:
  - psi4
dependencies:
    # Compute
  - psi4=1.2

    # Base depends
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine

    # Fireworks
    - fireworks
    - codecov
