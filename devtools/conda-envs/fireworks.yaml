name: test
channels:
  - psi4
dependencies:
    # Compute
  - psi4

    # Base depends
  - python
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema
  - bcrypt
  - cryptography

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    # Fireworks
    - fireworks

    - git+git://github.com/MolSSI/QCEngine@v0.4.0#egg=qcengine
    - codecov
    - mongoengine
    - pydantic
