name: test
channels:
  - conda-forge
  - psi4/label/dev
dependencies:

    # Base depends
  - numpy
  - pandas
  - pymongo
  - tornado
  - requests
  - jsonschema

    # Compute testing
  - psi4=1.2rc2
  - dask
  - distributed

    # Testing
  - pytest
  - codecov
  - pytest-cov
    # Pip depends
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine
