name: test
channels:
  - psi4
dependencies:
    # Compute
  - psi4=1.2

    # Dask testing environment
  - dask
  - distributed

    # Base depends
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema
  - bcrypt
  - cryptography

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine
    - codecov

      # Fireworks
    - fireworks
    
      # OpenFF Flows
    - git+git://github.com/lpwgroup/crank.git@v0.8.1#egg=crank
    - git+git://github.com/leeping/geomeTRIC@0.8.8#egg=geometric
