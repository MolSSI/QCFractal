name: test
channels:
  - conda-forge
  - psi4/label/dev
dependencies:
    # Computers
  - psi4=1.2

    # Dask
  - dask
  - distributed

    # Base depends
  - numpy
  - pandas
  - pymongo
  - tornado
  - requests
  - jsonschema

    # Testing
  - pytest
  - codecov
  - pytest-cov

    # Pip depends
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine

      # Fireworks
    - fireworks
    
      # OpenFF Flows
    - git+git://github.com/lpwgroup/crank#egg=crank
    - git+git://github.com/leeping/geomeTRIC#egg=geometric
