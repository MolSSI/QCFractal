name: test
channels:
  - psi4
dependencies:
    # Compute
  - psi4

    # Dask testing environment
  - dask
  - distributed

    # Base depends
  - python
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema
  - bcrypt
  - cryptography

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine
    - codecov
