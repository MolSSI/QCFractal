name: test
channels:
  - psi4
dependencies:
    # Compute
  - psi4=1.2

    # Dask testing environment
  - dask
  - distributed

    # Base depends
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine
    - codecov
