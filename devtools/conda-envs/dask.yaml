name: test
channels:
  - conda-forge
  - psi4/label/dev
dependencies:
    # Compute
  - psi4=1.2

    # Dask testing environment
  - dask
  - distributed

    # Base depends
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema

    # Testing
  - pytest
  - codecov
  - pytest-cov

    # Pip depends
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine
