name: qcarchive
channels:
  - defaults
  - conda-forge
dependencies:
  - python
  - numpy
  - pandas
  - mongodb
  - pymongo
  - tornado
  - requests
  - jsonschema
  - bcrypt
  - cryptography

# Test depends
  - pytest
  - pytest-cov
  - codecov

# Environment specific includes
  - rdkit

# QCArchive includes
  - qcengine>=0.5.1
  - qcelemental>=0.2.3

# Pip includes
  - pip:
    - pydantic
    - mongoengine
    - parsl
