name: qcfractal
channels:
  - defaults
  - conda-forge
dependencies:
  - python <=3.7
  - qcfractal
  - qcfractal-dashboard
  - qcengine >=0.13.0

    # Testing
  - pytest

    # Temporary pins
  - pint !=0.11